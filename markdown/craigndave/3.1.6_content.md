# 3.1 Computer architecture
*CPU instruction sets*

---

### What is an instruction set?

### Multiplying two numbers in memory

### Multiplying two numbers in memory
- Main memory (RAM)
- Registers
- Execution
- unit

### Multiplying two numbers in memory
- Main memory (RAM)
- Registers
- Execution
- unit

### Multiplying two numbers in memory
- Main memory (RAM)
- Registers
- Execution
- unit

### Our task is to find the product of two numbers:
  - The first is stored in memory location 0000.
  - The second is stored in memory location 0001.
  - The result of the calculation should be stored back in 0010.
- In a high-level language, this would be a line of code such as:
- product = num1 * num2
- Multiplying two numbers in memory
- Main memory (RAM)
- Registers
- Execution
- unit

### Multiplying two numbers in memory
- Main memory (RAM)
- Registers
- MULT 0000, 0001
- Execution
- unit

### Multiplying two numbers in memory
- Main memory (RAM)
- Registers
- LDA r000, 0000
- LDA r001, 0001
- PROD r000, r001
- STA 0010, r000
- Execution
- unit

### Instructions sets and mnemonics
- Example of a simplified instruction set
- Machine code that a particular CPU will recognise as part of its instruction set.
- These low-level instructions can be represented using mnemonics, which makes them easier for humans to understand.

### CPU instruction sets
- Processors execute machine code.
- Every type of processor has its own specific lists of commands that it can understand, called an instruction set.
- Machine code produced for one type of computer will not work on another – it is machine-specific.
- These specific machine codes can be represented using mnemonics, short codes like LDA.
- Mnemonics are easier for humans to understand and form the basis of a low-level programming language called assembly.
