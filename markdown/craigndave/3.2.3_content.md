# 3.2 Input and output devices
*Sensors – Part 1*

---

### Sensors
- Sensors are input devices. They come in different shapes and sizes, but they all measure physical properties of the environment and translate these measurements into digital information that a computer can use.

### Living in an analog world
- Analog signal

### Living in an analog world
- Digital signal
- On
- Off
- Off
- Analog signal

### From analog to digital
- Because real data is analog in nature, it is constantly changing and does not have a single discrete set of values.
- Consider this mercury thermometer. We simply look at the scale and use our best judgement to read the temperature. The final reading could be any one of an infinite number of values – it all depends how accurate we are when we read it.

### From analog to digital
- To make sense of physical, real-world quantities, all sensors convert the information they collect into a digital format – this is typically achieved using some form of analog-to-digital converter (ADC).
- These devices covert physical, real-world information into discrete digital values.

### Using sensors for monitoring and control
- Sensors record and send signals to a microprocessor
- If required, signals are converted to digital by an ADC
- Microprocessor analyses data by checking received values against stored values
- If values are outside the acceptable range, a warning is sent – e.g., on-screen/alarm
- If values are outside the acceptable range, a signal is sent to control a valve/motor
- Microprocessor has no effect on what is being monitored  by the sensor
- This change (output) affects the following set of input values received from sensors

### Sensors and the input/output cycle
- Here is an example of sensors being used in a smart braking system for a car.

### Sensors and the input/output cycle
- A sensor takes constant readings of the car’s distance to objects ahead of it.

### Sensors and the input/output cycle
- These analog, real-world values are converted to a format a microprocessor can understand using an analog-to-digital convertor (ADC).

### Sensors and the input/output cycle
- A microprocessor takes this input and calculates the speed the car is travelling and safe braking distances. If it detects the car is travelling too fast and a collision is likely, it issues commands to brake.

### Sensors and the input/output cycle
- Digital brake signals are converted into an analog form – these can then be sent to a motor (actuator), which will apply the brake via a digital-to-analog convertor (DAC).

### Sensors and the input/output cycle
- The brake slows the car down.

### Sensors and the input/output cycle
- The sensor continues to send out values. These will have changed due to the slowing of the car – this is known as feedback.

### Sensors
- Sensors can be designed to detect the levels of a wide variety of conditions.
- You are required to know about the following sensors for the exam.

### Sensors
- All sensors are input devices. They come in a variety of different shapes and sizes.
- Sensors measure physical properties of the environment. They then need to translate the information they collect into a digital form that a computer can use, achieved via an analog-to-digital convertor (ADC).
- Sensors that you may be examined on are limited to:
  - Acoustic
  - Accelerometer
  - Flow
  - Gas
  - Humidity
  - Infrared
  - Level
  - Light
  - Magnetic field
  - Moisture
  - pH
  - Pressure
  - Proximity
  - Temperature
