# 3.3 Data storage
*Primary storage,  RAM and ROM*

---

### Types of storage

### Types of storage
- RAM, ROM, registers and cache

### Types of storage
- Primary storage
- Holds the operating system, programs and data when they are not in use
- RAM, ROM, registers and cache

### Types of storage
- Primary storage
- Used for backing up and archiving large amounts of data
- Holds the operating system, programs and data when they are not in use
- RAM, ROM, registers and cache

### Types of storage
- Secondary storage
- Primary storage
  - Volatile – all except ROM
  - Relatively small capacity

### Types of storage
- Secondary storage
- Primary storage
- Non-volatile
- Much larger capacity
  - Volatile – all except ROM
  - Relatively small capacity

### Types of storage
- Secondary storage
- Primary storage
- Non-volatile
- Much larger capacity
- Slower access
  - Volatile – all except ROM
  - Relatively small capacity
  - Faster access

### RAM and ROM

### RAM and ROM

### RAM and ROM
- When a computer system first receives power, there are no instructions in the CPU.
- The system needs to load the operating system so it can start functioning.
- However, the operating system is stored on permanent secondary storage – the hard drive.
- At this stage, your computer isn’t even aware the hard drive exists.
- ROM
- CPU
- RAM
- Hard drive

### RAM and ROM
- The solution to this problem is ROM.
- A set of initial startup instructions called the bootstrap is placed on the ROM during manufacture.
- ROM is non-volatile, which means that the data it contains is retained when the computer is powered off.
- The first instruction in the bootstrap tells the computer to perform a power-on self-test (POST).
- The POST signals all connected components, wakes them up and makes the CPU aware of them.
- ROM
- CPU
- RAM
- Hard drive

### RAM and ROM
- The solution to this problem is ROM.
- A set of initial startup instructions called the bootstrap is placed on the ROM during manufacture.
- ROM is non-volatile, which means that the data it contains is retained when the computer is powered off.
- The first instruction in the bootstrap tells the computer to perform a power-on self-test (POST).
- The POST signals all connected components, wakes them up and makes the CPU aware of them.
- RAM

### RAM and ROM
- As the computer is now aware of the hard drive, it can load the operating system into RAM.
- The process of booting up the operating system can now begin.
- RAM

### RAM and ROM
- Primary storage: Any storage that can be directly accessed by the CPU – this includes:
  - Random-access memory (RAM)
  - Read-only memory (ROM)
  - Registers and cache
- This type of storage holds the data and instructions that the CPU needs to access while the computer is running. The CPU can access data from primary storage much faster than secondary storage.
- Random-access memory (RAM)
  - Holds programs and data currently in use by the CPU.
  - Volatile – contents are lost when the computer is powered off.
  - Read and write.
  - Larger than ROM.
- Read-only memory (ROM)
  - Holds startup instructions for the computer, known as the bootstrap.
  - In embedded systems, programs may by stored in ROM.
  - Non-volatile – contents are retained when the computer is powered off.
  - Read-only.
  - Smaller than RAM.
