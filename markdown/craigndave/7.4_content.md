# 7 Algorithm design and problem-solving
*Inputs, processes,  outputs and storage*

---

### Inputs, processes and outputs
- INPUT
- An electronic device that takes input…

### Inputs, processes and outputs
- INPUT
- PROCESS
- An electronic device that takes input, processes data…

### Inputs, processes and outputs
- OUTPUT
- INPUT
- PROCESS
- An electronic device that takes input, processes data and delivers output.

### Inputs, processes and outputs
- OUTPUT
- INPUT
- 5
- PROCESS
- An electronic device that takes input, processes data and delivers output.

### Inputs, processes and outputs
- OUTPUT
- INPUT
- 5
- PROCESS
- Multiply by 2
- An electronic device that takes input, processes data and delivers output.

### Inputs, processes and outputs
- OUTPUT
- 10
- INPUT
- 5
- PROCESS
- Multiply by 2
- An electronic device that takes input, processes data and delivers output.

### Inputs, processes and outputs
- OUTPUT
- INPUT
- PROCESS

### Inputs, processes and outputs
- OUTPUT
- INPUT
- PROCESS

### Inputs, processes and outputs
- OUTPUT
- INPUT
- PROCESS

### Inputs, processes, outputs and storage

### Inputs, processes, outputs and storage
- When writing a computer program, we need to be able to identify the inputs, processes, outputs and storage requirements of the problem.
- Under exam conditions, this will usually relate to a provided scenario.

### Inputs, processes, outputs and storage
- “Write a program to calculate the volume of a fish tank based on its dimensions, and report the result to the user.”

### Inputs, processes, outputs and storage
- Depth
- “Write a program to calculate the volume of a fish tank based on its dimensions, and report the result to the user.”
- Height
- Length

### Inputs, processes, outputs and storage
- Depth
- “Write a program to calculate the volume  of a fish tank based on its dimensions, report the result to the user and write the results to a text file.”
- Height
- Length

### Inputs, processes, outputs and storage
- “Write a program that asks the user for the number of students in their class and then prompts the user to enter each student’s test score within a range of 0–100. The program should then output the highest, lowest and average score to the user as well as saving there highest score to a file for future retrieval.”

### Inputs, processes, outputs and storage
- “Write a program that asks the user for the number of students in their class and then prompts the user to enter each student’s test score within a range of 0–100. The program should then output the highest, lowest and average score to the user as well as saving there highest score to a file for future retrieval.”

### Inputs, processes and outputs
- When tackling a problem, you need to consider any inputs, processes and outputs.
- Inputs
  - Anything that needs to be supplied to the program so it can meet its goals – often supplied by the user.
  - Consider an appropriate variable name and data type for inputs.
- Processes
  - Calculations that need to be performed while the program is running.
  - Consider whether any data need to change format or data type.
- Outputs
  - Consider what your program needs to output to the user.
  - Consider what form outputs should take.
  - Consider an appropriate variable name and data type for outputs.
- Storage
  - Consider any data that needs to be stored in files on an appropriate medium for use or retrieval in the future.
