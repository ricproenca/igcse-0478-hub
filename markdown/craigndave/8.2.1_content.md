# 8.2 Arrays
*The use of arrays*

---

### What is an array?
- An array can be thought of as a variable that can contain more than one data item – e.g., storing a list of names.

### What is an array?
- An array can be thought of as a variable that can contain more than one data item – e.g., storing a list of names.
- We can do that by allocating a contiguous part of memory for the purpose of storing that data.
- Contiguous means all the data is stored together, one element after the other.
- Note: Lists are different to arrays because lists are not contiguous.

### What is an array?
- An array can be thought of as a variable that can contain more than one data item – e.g., storing a list of names.
- We can do that by allocating a contiguous part of memory for the purpose of storing that data.
- Contiguous means all the data is stored together, one element after the other.
- Note: Lists are different to arrays because lists are not contiguous.
- This program will know the position in memory where the array starts – in this case, address 05.
- The program uses an index relative to this start point to allow us to easily access the array’s contents.
- So, “Jane” is at index 2.
- index
- [0]
- [1]
- [2]
- [3]

### One-dimensional arrays (Python)
- This line sets up a one-dimensional array of strings called “countries”.
- It assigns the initial values of:
- “Angola” at index 0
- “Austria” at index 1
- “Belgium” at index 2
- Note how indexes start at 0 and not 1.
- Python actually uses arrays but allows programmers to use them as lists. That’s why most people think Python only supports lists.

### One-dimensional arrays (Python)
- To output “Austria” from the array, we would need to use index 1, as index 1 represents the second item in the “countries” array.

### One-dimensional arrays (Python)
- Arrays are a static data structure – that means you can’t change the size of an array once you have set it up.
- This is a good example of Python allowing an array to behave like a list. Although you can’t tell (because it is abstracted from you), Python is actually moving the entire data structure to a new part of the memory to ensure it is contiguous if it needs to be.

### One-dimensional arrays (Python)
- Arrays are a static data structure – that means you can’t change the size of an array once you have set it up.
- This is a good example of Python allowing an array to behave like a list. Although you can’t tell (because it is abstracted from you), Python is actually moving the entire data structure to a new part of the memory to ensure it is contiguous if it needs to be.
- We can then insert an item into the new location at the end of the array using its index.

### Two-dimensional arrays (Python)
- You can visualise a two-dimensional array as a table with two sets of indexes – one for the rows and another for the columns.

### Two-dimensional arrays (Python)
- You can visualise a two-dimensional array as a table with two sets of indexes – one for the rows and another for the columns.  “countries[0][0]” contains “Angola”.

### Two-dimensional arrays (Python)
- You can visualise a two-dimensional array as a table with two sets of indexes – one for the rows and another for the columns.  “countries[0][0]” contains “Angola”.
- “countries[0][1]” contains “1246700”.

### Two-dimensional arrays (Python)
- You can visualise a two-dimensional array as a table with two sets of indexes – one for the rows and another for the columns.  “countries[0][0]” contains “Angola”.
- “countries[0][1]” contains “1246700”.
- “countries[1][1]” contains “83871”.

### Cambridge IGCSE pseudocode
- Here is an example of how these concepts will look in exam papers following the Cambridge IGCSE pseudocode format, which is listed towards the back of the syllabus.

### Arrays
- A static number of related data items stored in sequence within the same memory space.
- Each data item has the same (homogeneous) data type.
- A particular data item (element) is found using its index.
- Indexes usually start at 0 for the first data item (known as zero-indexed) but can also start from 1.
- Arrays may be single- or multi-dimensional.
- Visualise dimensions as a column (one-dimensional) or table (two-dimensional).
- Note: [1][0] could also be 112 depending on how you choose to store the data. It doesn’t matter whether you visualise rows or columns first, providing you are consistent in your program. Read the question carefully in an exam so you can be sure which way around the array is being stored.
- names[2] = “Sam”
- names[1][0] = “Dave”
