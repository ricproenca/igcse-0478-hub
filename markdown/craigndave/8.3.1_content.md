# 8.3 File handling
*The use of basic file  handling operations*

---

### This program asks for the name of a character, looks up the name of that character in a text file and outputs all their attributes:
- Health
- Stamina
- Hunger
- Reading data from text files
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()

### This program asks for the name of a character, looks up the name of that character in a text file and outputs all their attributes:
- Health
- Stamina
- Hunger
- Reading data from text files
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()

### Reading data from text files
- Our program requires the items from the text file to be in a very certain order – known as data dependency.
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()

### Reading data from text files
- First, we ask the user which character they want to output.
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()

### Reading data from text files
- Next, we open the file for reading. The open command takes two parameters:
- "characters.txt" – the name of the file to open.
- "r" – what we want to do with the file (e.g., read).
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()

### Reading data from text files
- Set a Boolean variable to false to indicate that we haven't yet reached the end of the file.
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()

### Reading data from text files
- Iterate through the file until such time as we reach the end of the file. We will change this end_of_file flag to true later on if we reach the end of the file.
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()

### We now read in four lines from the file, one after the other. Each line read in is placed into the appropriate variable:
- Name
- Health
- Stamina
- Hunger
- Reading data from text files
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()

### Reading data from text files
- Now we check if the character we read in from the file is the one we are looking for. If they are, we output the data.
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()

### Reading data from text files
- If the name we read in contains no data, we know we have reached the end of the file. In this case, we set the end_of_file flag to true, which will cause the WHILE loop to terminate.
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()

### Reading data from text files
- #Input the character to find in the file
- character = input("Which character do you wish to output? ")
- #Step 1 – Open the file for reading/writing
- f = open("characters.txt", "r")
- #Step 2 – Use a flag to indicate if the end of file is reached
- end_of_file = False
- #Step 3 – Iterate through all the data in the file
- while not end_of_file:
- name = f.readline().strip()
- health = f.readline().strip()
- stamina = f.readline().strip()
- hunger = f.readline().strip()
- #Step 4 – End of file or output data from file
- if character == name:
- print(name)
- print("Health:",health)
- print("Stamina:",stamina)
- print("Hunger:",hunger)
- if name == "":
- end_of_file = True
- #Step 5 – Close the file
- f.close()
- Finally, we need to close the file.

### Get the user to input the details for a character and store those details in variables:
- Name
- Health
- Stamina
- Hunger
- Writing data to text files
- name = input("Enter the name of the character: ")
- health = input("Enter the health of the character: ")
- stamina = input("Enter the stamina of the character: ")
- hunger = input("Enter the hunger of the character: ")
- #Step 1 – Open the file for characters
- f = open("characters.txt", "a")
- #Step 2 – Write the character data to the file
- f.write(name+"\n")
- f.write(health+"\n")
- f.write(stamina+"\n")
- f.write(hunger+"\n")
- #Step 3 – Close the file of characters
- f.close()

### Writing data to text files
- Next, we open the file for writing. The open command takes two parameters:
- "characters.txt" – the name of the file to open.
- "a" – what we want to do with the file (e.g., append).
- "w" – would create a new file/overwrite contents.
- name = input("Enter the name of the character: ")
- health = input("Enter the health of the character: ")
- stamina = input("Enter the stamina of the character: ")
- hunger = input("Enter the hunger of the character: ")
- #Step 1 – Open the file for characters
- f = open("characters.txt", "a")
- #Step 2 – Write the character data to the file
- f.write(name+"\n")
- f.write(health+"\n")
- f.write(stamina+"\n")
- f.write(hunger+"\n")
- #Step 3 – Close the file of characters
- f.close()

### Next, we write the contents of each variable on a new line in the characters text file.
- Note how we add the string "\n", which means start a new line – as if you had pressed enter.
- Writing data to text files
- name = input("Enter the name of the character: ")
- health = input("Enter the health of the character: ")
- stamina = input("Enter the stamina of the character: ")
- hunger = input("Enter the hunger of the character: ")
- #Step 1 – Open the file for characters
- f = open("characters.txt", "a")
- #Step 2 – Write the character data to the file
- f.write(name+"\n")
- f.write(health+"\n")
- f.write(stamina+"\n")
- f.write(hunger+"\n")
- #Step 3 – Close the file of characters
- f.close()

### Writing data to text files
- Finally, we need to close the file.
- name = input("Enter the name of the character: ")
- health = input("Enter the health of the character: ")
- stamina = input("Enter the stamina of the character: ")
- hunger = input("Enter the hunger of the character: ")
- #Step 1 – Open the file for characters
- f = open("characters.txt", "a")
- #Step 2 – Write the character data to the file
- f.write(name+"\n")
- f.write(health+"\n")
- f.write(stamina+"\n")
- f.write(hunger+"\n")
- #Step 3 – Close the file of characters
- f.close()

### Cambridge IGCSE pseudocode
- Here is an example of how these concepts will look in exam papers following the Cambridge IGCSE pseudocode format, which is listed towards the back of the syllabus.

### Cambridge IGCSE pseudocode
- Here is an example of how these concepts will look in exam papers following the Cambridge IGCSE pseudocode format, which is listed towards the back of the syllabus.

### Reading and writing data to text files
- The stages of writing data to a file are:
  - Open the file for creating/overwriting or appending to a file.
  - Write the data to the file.
  - Close the file.
- The stages of reading data from a file are:
  - Open the file for reading data.
  - Set a Boolean variable to false to indicate the end of file has not been reached.
  - While the end_of_file flag is false and the search item has not been found:
    - Read the data from the file.
    - If the data matches what is being searched for, assign the data to variables or output.
    - Check if the end of the file has been reached and if it has, set the Boolean variable to true.
  - Close the file.
